// To create package.json run npm init. Enter required values. This will create and initilize package.json
// To install package use npm install moment --save, This add an entry to package.json 
// For dev depenencies use npm install jasmine-node --save-dev
// For global install npm install -g nodemon
var moment = require('moment');
console.log(moment().format());